/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { WccIntervention_undertakenService } from './wcc-intervention_undertaken.service';

describe('Service: WccIntervention_undertaken', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [WccIntervention_undertakenService]
    });
  });

  it('should ...', inject([WccIntervention_undertakenService], (service: WccIntervention_undertakenService) => {
    expect(service).toBeTruthy();
  }));
});
